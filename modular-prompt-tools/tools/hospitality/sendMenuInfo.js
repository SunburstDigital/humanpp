/**
 * Send menu information to a customer.
 * Params: { phone, email, menuType }
 * Returns: { ok: true, data } or { ok: false, error }
 * Only used in hospitality flows.
 */
module.exports = async function sendMenuInfo(params) {
    // TODO: Implement menu info sending logic
    return { ok: false, error: 'Not implemented yet' };
};