/**
 * Send demo information to a lead.
 * Params: { leadId, email, infoType }
 * Returns: { ok: true, data } or { ok: false, error }
 * Only used in Sunburst flows.
 */
module.exports = async function sendDemoInfo(params) {
    // TODO: Implement demo info sending logic
    return { ok: false, error: 'Not implemented yet' };
};